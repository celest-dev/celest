import 'package:test/test.dart';

void main() {
  test('test name', () {
    // final registry = ExtensionRegistry();
    // Cloudevent.registerAllExtensions(registry);
    // final descriptor = DescriptorProto.fromBuffer(
    //   userCreatedEventDescriptor,
    //   registry,
    // );
    // final type = descriptor.options.getExtension(Cloudevent.cloudEventType);
    // expect(type, 'celest.cloud.auth.v1.userCreated');
  });
}
