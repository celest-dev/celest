name: example_client
description: The Celest client for example.
publish_to: none

environment:
  sdk: ^3.7.0

dependencies:
  celest: ^1.0.0
  celest_backend:
    path: ../
  celest_core: ^1.0.0
  http: ^1.0.0
  native_storage: ^0.3.0

dev_dependencies:
  lints: ^5.0.0
  test: ^1.25.0
dependency_overrides: {}
